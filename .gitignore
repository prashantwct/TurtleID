
# Never commit field records or locality data
*.jsonl
*.pyc
.DS_Store
.env
.venv/
__pycache__/
contributions/
contributions/images/
data/iucn_cache.json
dataset/
logs/
models/*.pt
models/*.npz
models/calibration.json
models/runs/
negatives/
records/

# Reference photographs are tracked, with per-image credit and rights recorded
# in data/reference_images.json. See PUBLISHING.md.

# Field photographs awaiting labelling. Never tracked: they carry locality data
# in EXIF and in the frame itself, and a coordinate for a nesting bank is the
# one thing this project must not publish.
pool/

# The hand-arranged drop folder read by training/import_folders.py. These are
# the originals, still carrying every EXIF tag their camera wrote.
incoming/
